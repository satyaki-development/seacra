var settings = {
    uid: "cddee84543508dbde2308ae5f3fcdf57", 
    huid: "486925",
    source: "Shopify"{% if customer  %},
    vid: "{{ customer.id }}"{% endif %},
    options: {}     
};

var ratingSettings = {
    title: "{{ product.title | escape }}",
    url: "{{ page.url }}",
    domain: "{{ shop.permanent_domain | escape }}",
    img: "{{ product.featured_image | product_img_url: 'large'  |replace: '?', '%3F' | replace: '&','%26'}}",
    tags: "{% for tag in product.tags %}{{ tag | escape }};{% endfor %}"
};